Chain Default {
    State A {
        move_to = {
            1 = A
        }
        drop = 0
        delay = 0
        time_factor = 100000
    }
}

Chain Vipe {
    State Start {
        time_factor = 2000
        move_to = {
            0.8 = Start
            0.2 = B
        }        
    }
    State B {
        move_to = {
            1 = B
        }
        #delay = 0.1
        delay = 1

        delay_type = U(0,3000)        
        #delay_type = U(1000,1000);

        time_factor = 50000

        stable_delay = 0
    }
   
    # source works
    # dest works
    # length works
    # protocol works
    # tos works
    #
    
    #source = 127.0.0.1
    #tos = 24
    #length = l < 500

    #protocol = udp
 
}

